{
  "name": "firmware-processor-lambda",
  "version": "1.0.0",
  "description": "Lambda function to process firmware uploads - generates manifests and cleans up old versions",
  "main": "src/handler.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.7.9",
    "typescript": "^5.6.3"
  }
}
